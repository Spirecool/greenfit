{% extends 'base.html.twig' %}

{% block title %}Fiche partenaire {% endblock %}

{% block body %}
    <h1>Fiche Partenaire {{ partner.slug }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Est actif</th>
                <td>{{ partner.isActive ? 'Oui' : 'Non' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ partner.slug }}</td>
            </tr>
            <tr>
                <th>Modules</th>
                <td>
                    {% if partner.Modules %}
                    {% for module in partner.Modules %}
                    {{module}}
                    {% endfor %}
                    {% endif %}
                </td>
            </tr>    
        </tbody>
    </table>

    <a href="{{ path('app_partners_index') }}">back to list</a>

    <a href="{{ path('app_partners_edit', {'id': partner.id}) }}">edit</a>

    {{ include('partners/_delete_form.html.twig') }}
{% endblock %}

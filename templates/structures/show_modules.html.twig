{% extends 'base.html.twig' %}

{% block title %}Fiche Structure{% endblock %}

{% block body %}
  
       <h1 class="py-5">Fiche Structure : <span class="heading-color"> {{structure.name}} </span> : Partenaires</span></h1>

    <table class="table">
        <tbody>
                
             <tr>
                <th scope="col">Modules activés</th> </tr>
            <tr>  
                    {% for module in modules %}
                    <tr>
                    <td>
                    {{module.name}}
                    </td>
                    {% for m in structure.modules %}
                    <td>
                    {{module.id == m.id ? '<div class="on ">Oui</div>' : '<div class="off ">Non</div>'}}
                    </td>
                    {% else %}
                    <td>non</td>
                    </tr>
                    {% endfor %}
                    {% endfor %}
            </tr>
        </tbody>
    </table>


    
{% endblock %}
